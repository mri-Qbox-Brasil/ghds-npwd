import React from 'react';
import { AppWrapper } from '@ui/components';
import { AppContent } from '@ui/components/AppContent';

export const LocationApp: React.FC = () => {
  return (
    <AppWrapper id="location">
      <AppContent />
    </AppWrapper>
  );
};
